import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.happystarfishchase',
  appName: 'happy-starfish-chase',
  webDir: 'dist'
};

export default config;
